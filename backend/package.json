{
  "name": "@token-meter/tracker",
  "version": "0.1.0",
  "description": "Local backend service that tracks Claude API token consumption",
  "main": "dist/server.js",
  "bin": {
    "token-meter-tracker": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node": "^10.9.2"
  }
}
